import { makeStyles } from '@material-ui/styles'

export const useStyles = makeStyles((theme) => ({
    pit: { boraderWidth: 1, borderStyle: 'solid', borderColor: 'black', borderRadius: '50%', width: 25, height: 25 },
    kalaha: { boraderWidth: 1, borderStyle: 'solid', borderColor: 'black', borderRadius: '50%', width: 45, height: 45 },
}))